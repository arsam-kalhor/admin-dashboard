# Generate a long random value for each deployment, for example:
# openssl rand -base64 48
AUTH_SECRET=replace-with-a-long-random-secret

# Public deployment origin for absolute social preview image URLs.
# Vercel deployments can derive this automatically; set it for a custom domain.
# NEXT_PUBLIC_SITE_URL=https://your-domain.example
